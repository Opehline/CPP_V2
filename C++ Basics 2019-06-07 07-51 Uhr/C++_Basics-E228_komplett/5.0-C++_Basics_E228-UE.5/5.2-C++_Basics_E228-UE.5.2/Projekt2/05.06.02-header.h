// * Globale Variablen werden in einem separaten Speicherplazt gelegt.
// * Globale Variablen werden immer mit NULL initialisiert.
// * Gueltig in allen Compilern!


// * Richtig:
extern float ergebnis;

// * Richtig:
void summe(float a, float b);
void multiplikation(float a, float b);
void division(float a, float b);
